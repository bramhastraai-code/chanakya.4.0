export enum RequirementStatus {
  OPEN = 'open',
  ACCEPTED = 'accepted',
  IN_PROGRESS = 'in_progress',
  MATCHED = 'matched',
  CLOSED = 'closed',
}

export enum TransactionType {
  BUYING = 'buying',
  SELLING = 'selling',
  RENTING = 'renting',
}
